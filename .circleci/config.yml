version: 2.1
jobs:
  build:
    docker:
      - image: circleci/openjdk
    steps:
      - checkout
      - run: mvn clean install
        
      #- maven/test # checkout, build, test, and upload test results
